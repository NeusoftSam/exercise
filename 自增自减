#include<stdio.h>
main()
{
	int count=0; 
	while(count<500)
	{
		count=count++>count?count++:count;
		printf("%d\n",count);
		while(count==500)
		{
			while(count>0)
		{
			count=count--<count?count--:count;
			printf("%d\n",count);
		}
	}
 }
}
